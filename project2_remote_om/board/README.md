# Board Programs

Board-side code is split into:

- `device_agent/`: remote O&M agent for network communication, status
  reporting, remote commands, logs, service management, and OTA triggers
- `power_manager/`: low-power mode manager for mode switching,
  suspend/resume, and wakeup recovery
- `scripts/`: installation, startup, and packaging helper scripts

